/**
 * PostCSS configuration file.
 *
 * Tailwind CSS uses PostCSS to process its output. Autoprefixer adds
 * vendor prefixes to ensure broad browser compatibility.
 */
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};